# Agents (project)

This repo uses a **condensed** Claude Code layout aligned with `EVERYTHING-CLCODE_NODE_CONDENSE_STRATEGY.txt`.

- Subagent definitions: `agents/*.md`
- Primary workflow docs: `skills/*/SKILL.md`
- Slash commands: `commands/*.md`

Spawn **planner** for implementation breakdown when scope spans multiple packages or directories.
