---
description: Print or refine an implementation plan (aligns with agents/planner)
---

# /plan

Use the **planner** agent and `skills/harness-workflow` for scope. For the resolved install module list, run `npm run plan` in the repo root.
