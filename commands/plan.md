---
description: Print or refine an implementation plan (aligns with agents/planner)
---

# /plan

Use the **planner** agent and `skills/harness-workflow` for scope. Keep harness work aligned with **`MONOREPO_STACK_HARNESS_GAP_ANALYSIS.txt`** (backlog and deliverables) and **`HARNESS_NODE_CONDENSE_STRATEGY.txt`**.

For the resolved install module list for this repo, run `npm run plan` at the repo root (or `node scripts/install-plan-stub.js <profile>`).
