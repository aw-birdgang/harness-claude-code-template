# CLAUDE.md

## Project

**harness-claude-code-template** — minimal ECC-shaped harness (Node-only scripts, single canonical `skills/` tree).

## Commands

- `npm test` — validate skeleton (manifests + required dirs)
- `npm run plan` — print JSON install plan for profile `minimal` (override: `node scripts/install-plan-stub.js core`)
- `node scripts/ecc.js help`

## Conventions

See `EVERYTHING-CLCODE_NODE_CONDENSE_STRATEGY.txt` for shrink axes and phased roadmap.
